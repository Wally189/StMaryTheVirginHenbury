---
import { getCollection, type CollectionEntry } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { formatDateNoWeekday } from "../../utils/dates";

export async function getStaticPaths() {
  const news = (await getCollection("news")).filter((item) => !item.data.draft);
  return news.map((item) => ({
    params: { slug: item.slug },
    props: { item },
  }));
}

interface Props {
  item: CollectionEntry<"news">;
}

const { item } = Astro.props;
const { Content } = await item.render();
---

<BaseLayout title={`${item.data.title} | St Mary the Virgin, Henbury`} description={item.data.summary}>
  <article class="container-width mt-10 max-w-4xl">
    <p class="text-sm text-stone-700">{formatDateNoWeekday(item.data.date)} | {item.data.category}</p>
    <h1 class="mt-2 text-5xl">{item.data.title}</h1>
    {item.data.image && (
      <img
        src={item.data.image.src}
        alt={item.data.image.alt}
        class="mt-5 h-80 w-full rounded-lg border border-stone-300 object-cover"
        width="1200"
        height="700"
        loading="lazy"
      />
    )}
    <div class="mt-6 space-y-4 text-lg text-stone-800">
      <Content />
    </div>
    <p class="mt-8">
      <a href="/news">Back to all news</a>
    </p>
  </article>
</BaseLayout>
