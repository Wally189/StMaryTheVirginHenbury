---
import { getEntry } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHero from "../components/PageHero.astro";

const page = await getEntry("pages", "life-events");
if (!page) throw new Error("Missing life events content.");
const { Content } = await page.render();
---

<BaseLayout title="Life Events | St Mary the Virgin, Henbury" description={page.data.intro}>
  <PageHero
    title={page.data.title}
    intro={page.data.intro}
    image="/images/henbury-church-cross.jpg"
    imageAlt="Churchyard cross with St Mary's in the background"
  />
  <section class="container-width mt-12">
    <ul class="grid gap-6 md:grid-cols-3">
      {
        page.data.cards?.map((card) => (
          <li class="card">
            <p class="eyebrow">Life event</p>
            <h2 class="text-2xl">{card.title}</h2>
            <p class="mt-2 text-nave-900">{card.text}</p>
            {card.linkHref && card.linkText && (
              <p class="mt-4">
                <a href={card.linkHref}>{card.linkText}</a>
              </p>
            )}
          </li>
        ))
      }
    </ul>
  </section>
  <section class="container-width mt-12">
    <div class="section-rule text-nave-900">
      <Content />
    </div>
  </section>
</BaseLayout>
